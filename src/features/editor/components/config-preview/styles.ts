import styled from "styled-components";

export const ConfigPreviewStyles = styled.div`
  background: #fff;
  padding: 10px;
  color: #4f4e55;
  border-radius: 10px;
  margin-top: 40px;
  text-align: left;
  transition: 0.4s;

  &:hover {
    transition: 0.4s;
    transform: scale(1.06);
  }

  button {
    width: 100%;
  }
`;
